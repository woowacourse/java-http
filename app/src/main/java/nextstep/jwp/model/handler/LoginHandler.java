package nextstep.jwp.model.handler;

import nextstep.jwp.model.JwpHttpRequest;

import java.io.OutputStream;

public class LoginHandler implements CustomHandler {

    private static final String LOGIN_PAGE_PATH = "index.html";

    @Override
    public void handle(JwpHttpRequest uri, OutputStream outputStream) {
//        String line = reader.readLine();
//        while (!line.isBlank()) {
//            log.debug(line);
//            line = reader.readLine();
//        }
//
//        final String responseBody = "Hello world!";
//        final String response = String.join("\r\n",
//                "HTTP/1.1 200 OK ",
//                "Content-Type: text/html;charset=utf-8 ",
//                "Content-Length: " + resourceFile.getBytes().length + " ",
//                "",
//                resourceFile);
    }
}
